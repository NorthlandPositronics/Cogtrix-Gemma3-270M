# Python dependencies for Gemma 3 270M minimal container (CPU-only)
# Pinned versions for reproducibility and minimal size

# Core ML framework - CPU ONLY
# amd64: pip resolves torch+cpu from PyTorch CDN (no CUDA stubs, ~600MB smaller)
# arm64: falls back to PyPI regular wheel (arm64 has no CUDA support anyway)
torch>=2.4.0
--extra-index-url https://download.pytorch.org/whl/cpu

# Hugging Face Transformers - 4.50+ required for Gemma 3 support
transformers>=4.50.0

# Safetensors for efficient model loading
safetensors>=0.5.0

# Hugging Face hub for model downloads
huggingface-hub>=0.28.0

# API server dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
